version: 1

# Mirrors the desired Tendril project definition for the authoritative
# Cacophony projects.yaml entry.
projects:
  - name: tendril
    imports:
      - project.yaml
    remote: "ssh://git@github.com/harryaskham/tendril.git"
    default_branch: main
    identity: harryaskham
    beads:
      enabled: true
      branch: beads
